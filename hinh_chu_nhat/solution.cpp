#include <bits/stdc++.h>

#define FASTIO ios_base::sync_with_stdio(false); cin.tie(NULL);

using namespace std;

int main() {
    FASTIO;

    freopen("data.txt", "r", stdin);

    int n, m, q;
    cin >> n >> m >> q;

    

    return 0;
}